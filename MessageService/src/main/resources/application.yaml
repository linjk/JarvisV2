server:
  port: 9000

spring:
  cloud:
    nacos:
      discovery:
        service: message-service
      server-addr: localhost:8848
  application:
    name: message-center

dubbo:
  application:
    name: message-center
    id: message-center
  registry:
    address: nacos://localhost:8848
  protocol:
    name: dubbo
    port: 20448