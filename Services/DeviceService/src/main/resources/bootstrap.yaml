spring:
  cloud:
    nacos:
      discovery:
        service: user-service
      server-addr: localhost:8848
      config:
        enabled: true
        file-extension: yaml
        server-addr: localhost:8848
  application:
    name: user-service
  profiles:
    active: sit