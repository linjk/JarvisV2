spring:
  cloud:
    nacos:
      discovery:
        service: device-service
      server-addr: localhost:8848
      config:
        enabled: true
        file-extension: yaml
        server-addr: localhost:8848
  application:
    name: device-service
  profiles:
    active: sit